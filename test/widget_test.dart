import 'package:flutter_test/flutter_test.dart';
import 'package:flutter/material.dart';
import 'package:rps_arena/main.dart';

void main() {
  test('MyApp is a StatelessWidget', () {
    expect(MyApp(), isA<StatelessWidget>());
  });
}
